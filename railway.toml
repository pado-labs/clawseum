[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter @clawseum/api build"

[deploy]
startCommand = "pnpm --filter @clawseum/api start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
